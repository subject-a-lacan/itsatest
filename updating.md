# 更新记录

## 非数值处理

- `main.py` 训练开始：追加写 `shuju.txt`。
- `main.py` 每10轮：记录训练损失、频率误差、成功率、均方根误差。
- `main.py` 参数：新增 `--loss_lambda`，替代写死权重。
- `main.py` 参数：新增 `--grad_clip`，默认梯度裁剪 `0.5`。
- `main.py` 训练：损失或梯度异常就记录并停止。
- `main.py` 测试：输出异常就跳过。
- `main.py` 测试：矩阵铅笔法报错就跳过。
- `main.py` 测试：平均损失只算成功批次。
