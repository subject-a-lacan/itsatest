关键：squareplus 不是线性的
你说的"相对量级一样就行"，对线性函数是对的。比如 y = 3x，输入缩小 50 倍，输出也缩小 50 倍，信息完全保留。

但 squareplus 不是线性的。它的公式是：


squareplus(w) = (w - √(w² + 0.01)) / 2
这里面有一个固定的 0.01，它不随信号缩放。

具体算一下
假设 U 被压了 50 倍，导致 w 也从正常的 0.1 被压到了 0.002。

正常尺度下 (w ≈ 0.1)：


符号正确 (w = +0.1)：squareplus = (0.1 - √(0.01+0.01)) / 2
                              = (0.1 - 0.141) / 2 = -0.020  ← 惩罚小

符号错误 (w = -0.1)：squareplus = (-0.1 - √(0.01+0.01)) / 2
                              = (-0.1 - 0.141) / 2 = -0.121  ← 惩罚大
正确和错误差了 6 倍。 梯度知道往哪走。

当前尺度下 (w ≈ 0.002)：


符号正确 (w = +0.002)：squareplus = (0.002 - √(0.000004+0.01)) / 2
                                 ≈ (0.002 - 0.1) / 2 = -0.049

符号错误 (w = -0.002)：squareplus = (-0.002 - √(0.000004+0.01)) / 2
                                 ≈ (-0.002 - 0.1) / 2 = -0.051
正确和错误只差了 4%。 梯度几乎分不出对错。

为什么会这样
因为 √(w² + 0.01) 这一项里，0.01 是固定的。当 w 太小（0.002）时，w² 只有 0.000004，根本影响不了 √ 的结果。√ 永远 ≈ 0.1。

所以 squareplus 的输出 ≈ (w - 0.1) / 2。这里面：

-0.1/2 = -0.05 是固定偏移，每个点都一样
w/2 才是携带"频率对不对"信息的部分
当前 w ≈ 0.002，信息部分只占固定偏移的 4%。信号被淹没在固定的背景值里了。

一句话
squareplus 里面那个 0.01 是一个"检测阈值"。信号比阈值小 50 倍的时候，它就分不出对错了——不管你相对比例对不对